version: '3.9'
services:
  web:
    build: .
    ports:
      - '5000:5000'
    environment:
      - FLASK_ENV=development
      - SECRET_KEY=supersecretkey
    volumes:
      - .:/app
      - ./static/uploads:/app/static/uploads
